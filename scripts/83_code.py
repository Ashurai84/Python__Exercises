# 84 Write a program that appends a user-input line to an existing file without overwriting it.

def sum_dict_values(d):
    return sum(d.values())

# Example usage
my_dict = {"a": 1, "b": 2, "c": 3}
total_sum = sum_dict_values(my_dict)
print("Sum of dictionary values:", total_sum)
