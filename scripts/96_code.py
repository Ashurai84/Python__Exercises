# 97 	Create a class Point that overloads the + operator (using __add__) to add the coordinates of two Point objects.

def concatenate_lists(lst1, lst2):
    return lst1 + lst2

# Example usage
list1 = [1, 2, 3]
list2 = [4, 5, 6]
print("Concatenated list:", concatenate_lists(list1, list2))
