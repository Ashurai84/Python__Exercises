#95 Create a class Counter with a class variable count. Implement a @classmethod to increment count and a @staticmethod to display some utility message.

def all_unique(lst):
    return len(lst) == len(set(lst))

# Example usage
numbers = [1, 2, 3, 4, 5]
print("Are all elements unique?", all_unique(numbers))
