# 98 Use the abc module to define an abstract base class Shape with an abstract method area(). Implement subclasses Circle and Rectangle.

def is_list_empty(lst):
    return len(lst) == 0

# Example usage
numbers = []
print("Is the list empty?", is_list_empty(numbers))
